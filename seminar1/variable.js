/**
 * var
 */
var myName = "소현";
console.log(`${myName} is wrong name!`);

var myName = "김소현";
console.log(`My name is ${myName}`);


/**
 * let
 */
let part = "serverrr";
// let part = "server2"; // 재할당은 가능하지만 재선언은 불가
part = "server";
console.log(`Let's go ${part}`)


/**
 * const
 */
const school = "sopt";
// school = "sopt"; // 재할당도 불가능
console.log(`school ${part}`);